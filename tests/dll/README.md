# MSVC test project

This directory contains an MSVC project designed to verify linking against the routex client DLL in both Debug and Release configurations. The project produces a secondary DLL that integrates the routex client.

`test.ps1` can be used to check whether the built DLL works.
